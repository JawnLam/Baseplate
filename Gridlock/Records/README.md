# Records — not needed for construction; do not modify

This folder is the archived history of how the Gridlock founding documents were designed: the product interview (`_product-interview.md`), the document-set selection rationale (`_selection-record.md`), dependency verifications (`_dependency-log.md`), the design-state journal (`_design-state.md`), the cartridge manifest (`_ov-manifest.md`), all session logs (`Sessions/`), and the gate record (`stranger-test-log.md` — consistency audit + stranger test, verdict SHIP on run 1).

**A builder never needs anything in here.** Everything required to build Gridlock lives in `../Construction/`, indexed by `../AI-BOOTSTRAP.md`. These files are immutable history: read for provenance audits; never edit, never delete.
